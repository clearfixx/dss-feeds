/**
 * Internal foundation marker.
 *
 * The public contracts are introduced in the next vertical slice after the
 * workspace pipeline is verified.
 */
export const DSS_FEEDS_CORE_VERSION = '0.0.0' as const
